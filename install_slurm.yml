---
- hosts: all
  become: true

  vars_files:
    - ./vars.yml

  tasks:

  - name: Update apt
    apt:
      update_cache: yes

  - name: Ensure required softwares are installed
    apt:
      name: "{{ item }}"
      state: present
    with_items: "{{ pre_req_softwares }}"


  
